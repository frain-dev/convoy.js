/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 100718f5f5bd
 */

import * as z from "zod/v4-mini";
import { ConvoyCore } from "../core.js";
import { encodeJSON, encodeSimple } from "../lib/encodings.js";
import { matchStatusCode } from "../lib/http.js";
import * as M from "../lib/matchers.js";
import { compactMap } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { RequestOptions } from "../lib/sdks.js";
import { extractSecurity, resolveGlobalSecurity } from "../lib/security.js";
import { pathToFunc } from "../lib/url.js";
import { ConvoyError } from "../models/errors/convoy-error.js";
import {
  ConnectionError,
  InvalidRequestError,
  RequestAbortedError,
  RequestTimeoutError,
  UnexpectedClientError,
} from "../models/errors/http-client-errors.js";
import * as errors from "../models/errors/index.js";
import { ResponseValidationError } from "../models/errors/response-validation-error.js";
import { SDKValidationError } from "../models/errors/sdk-validation-error.js";
import * as models from "../models/index.js";
import * as operations from "../models/operations/index.js";
import { APICall, APIPromise } from "../types/async.js";
import { Result } from "../types/fp.js";

/**
 * Test a filter
 *
 * @remarks
 * This endpoint tests a filter against a payload
 */
export function filtersTestFilter(
  client: ConvoyCore,
  projectID: string,
  subscriptionID: string,
  eventType: string,
  body: models.ModelsTestFilterRequest,
  options?: RequestOptions,
): APIPromise<
  Result<
    operations.TestFilterResponse,
    | errors.TestFilterBadRequestError
    | errors.TestFilterUnauthorizedError
    | errors.TestFilterNotFoundError
    | ConvoyError
    | ResponseValidationError
    | ConnectionError
    | RequestAbortedError
    | RequestTimeoutError
    | InvalidRequestError
    | UnexpectedClientError
    | SDKValidationError
  >
> {
  return new APIPromise($do(
    client,
    projectID,
    subscriptionID,
    eventType,
    body,
    options,
  ));
}

async function $do(
  client: ConvoyCore,
  projectID: string,
  subscriptionID: string,
  eventType: string,
  body: models.ModelsTestFilterRequest,
  options?: RequestOptions,
): Promise<
  [
    Result<
      operations.TestFilterResponse,
      | errors.TestFilterBadRequestError
      | errors.TestFilterUnauthorizedError
      | errors.TestFilterNotFoundError
      | ConvoyError
      | ResponseValidationError
      | ConnectionError
      | RequestAbortedError
      | RequestTimeoutError
      | InvalidRequestError
      | UnexpectedClientError
      | SDKValidationError
    >,
    APICall,
  ]
> {
  const input: operations.TestFilterRequest = {
    projectID: projectID,
    subscriptionID: subscriptionID,
    eventType: eventType,
    body: body,
  };

  const parsed = safeParse(
    input,
    (value) => z.parse(operations.TestFilterRequest$outboundSchema, value),
    "Input validation failed",
  );
  if (!parsed.ok) {
    return [parsed, { status: "invalid" }];
  }
  const payload = parsed.value;
  const body$ = encodeJSON("body", payload.body, { explode: true });

  const pathParams = {
    eventType: encodeSimple("eventType", payload.eventType, {
      explode: false,
      charEncoding: "percent",
    }),
    projectID: encodeSimple("projectID", payload.projectID, {
      explode: false,
      charEncoding: "percent",
    }),
    subscriptionID: encodeSimple("subscriptionID", payload.subscriptionID, {
      explode: false,
      charEncoding: "percent",
    }),
  };
  const path = pathToFunc(
    "/v1/projects/{projectID}/subscriptions/{subscriptionID}/filters/test/{eventType}",
  )(pathParams);

  const headers = new Headers(compactMap({
    "Content-Type": "application/json",
    Accept: "application/json",
  }));

  const secConfig = await extractSecurity(client._options.apiKeyAuth);
  const securityInput = secConfig == null ? {} : { apiKeyAuth: secConfig };
  const requestSecurity = resolveGlobalSecurity(securityInput);

  const context = {
    options: client._options,
    baseURL: options?.serverURL ?? client._baseURL ?? "",
    operationID: "TestFilter",
    oAuth2Scopes: null,

    resolvedSecurity: requestSecurity,

    securitySource: client._options.apiKeyAuth,
    retryConfig: options?.retries
      || client._options.retryConfig
      || { strategy: "none" },
    retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
  };

  const requestRes = client._createRequest(context, {
    security: requestSecurity,
    method: "POST",
    baseURL: options?.serverURL,
    path: path,
    headers: headers,
    body: body$,
    userAgent: client._options.userAgent,
    timeoutMs: options?.timeoutMs || client._options.timeoutMs || -1,
  }, options);
  if (!requestRes.ok) {
    return [requestRes, { status: "invalid" }];
  }
  const req = requestRes.value;

  const doResult = await client._do(req, {
    context,
    isErrorStatusCode: (statusCode: number) =>
      matchStatusCode({ status: statusCode } as Response, ["4XX", "5XX"]),
    retryConfig: context.retryConfig,
    retryCodes: context.retryCodes,
  });
  if (!doResult.ok) {
    return [doResult, { status: "request-error", request: req }];
  }
  const response = doResult.value;

  const responseFields = {
    HttpMeta: { Response: response, Request: req },
  };

  const [result] = await M.match<
    operations.TestFilterResponse,
    | errors.TestFilterBadRequestError
    | errors.TestFilterUnauthorizedError
    | errors.TestFilterNotFoundError
    | ConvoyError
    | ResponseValidationError
    | ConnectionError
    | RequestAbortedError
    | RequestTimeoutError
    | InvalidRequestError
    | UnexpectedClientError
    | SDKValidationError
  >(
    M.json(200, operations.TestFilterResponse$inboundSchema),
    M.jsonErr(400, errors.TestFilterBadRequestError$inboundSchema),
    M.jsonErr(401, errors.TestFilterUnauthorizedError$inboundSchema),
    M.jsonErr(404, errors.TestFilterNotFoundError$inboundSchema),
    M.fail("4XX"),
    M.fail("5XX"),
  )(response, req, { extraFields: responseFields });
  if (!result.ok) {
    return [result, { status: "complete", request: req, response }];
  }

  return [result, { status: "complete", request: req, response }];
}
