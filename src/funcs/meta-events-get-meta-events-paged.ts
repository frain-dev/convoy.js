/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 7daf9301c694
 */

import * as z from "zod/v4-mini";
import { ConvoyCore } from "../core.js";
import { encodeFormQuery, encodeSimple } from "../lib/encodings.js";
import { matchStatusCode } from "../lib/http.js";
import * as M from "../lib/matchers.js";
import { compactMap } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { RequestOptions } from "../lib/sdks.js";
import { extractSecurity, resolveGlobalSecurity } from "../lib/security.js";
import { pathToFunc } from "../lib/url.js";
import { ConvoyError } from "../models/errors/convoy-error.js";
import {
  ConnectionError,
  InvalidRequestError,
  RequestAbortedError,
  RequestTimeoutError,
  UnexpectedClientError,
} from "../models/errors/http-client-errors.js";
import * as errors from "../models/errors/index.js";
import { ResponseValidationError } from "../models/errors/response-validation-error.js";
import { SDKValidationError } from "../models/errors/sdk-validation-error.js";
import * as operations from "../models/operations/index.js";
import { APICall, APIPromise } from "../types/async.js";
import { Result } from "../types/fp.js";

/**
 * List all meta events
 *
 * @remarks
 * This endpoint fetches meta events with pagination
 */
export function metaEventsGetMetaEventsPaged(
  client: ConvoyCore,
  request: operations.GetMetaEventsPagedRequest,
  options?: RequestOptions,
): APIPromise<
  Result<
    operations.GetMetaEventsPagedResponse,
    | errors.GetMetaEventsPagedBadRequestError
    | errors.GetMetaEventsPagedUnauthorizedError
    | errors.GetMetaEventsPagedNotFoundError
    | ConvoyError
    | ResponseValidationError
    | ConnectionError
    | RequestAbortedError
    | RequestTimeoutError
    | InvalidRequestError
    | UnexpectedClientError
    | SDKValidationError
  >
> {
  return new APIPromise($do(
    client,
    request,
    options,
  ));
}

async function $do(
  client: ConvoyCore,
  request: operations.GetMetaEventsPagedRequest,
  options?: RequestOptions,
): Promise<
  [
    Result<
      operations.GetMetaEventsPagedResponse,
      | errors.GetMetaEventsPagedBadRequestError
      | errors.GetMetaEventsPagedUnauthorizedError
      | errors.GetMetaEventsPagedNotFoundError
      | ConvoyError
      | ResponseValidationError
      | ConnectionError
      | RequestAbortedError
      | RequestTimeoutError
      | InvalidRequestError
      | UnexpectedClientError
      | SDKValidationError
    >,
    APICall,
  ]
> {
  const parsed = safeParse(
    request,
    (value) =>
      z.parse(operations.GetMetaEventsPagedRequest$outboundSchema, value),
    "Input validation failed",
  );
  if (!parsed.ok) {
    return [parsed, { status: "invalid" }];
  }
  const payload = parsed.value;
  const body = null;

  const pathParams = {
    projectID: encodeSimple("projectID", payload.projectID, {
      explode: false,
      charEncoding: "percent",
    }),
  };
  const path = pathToFunc("/v1/projects/{projectID}/meta-events")(pathParams);

  const query = encodeFormQuery({
    "direction": payload.direction,
    "endDate": payload.endDate,
    "next_page_cursor": payload.next_page_cursor,
    "perPage": payload.perPage,
    "prev_page_cursor": payload.prev_page_cursor,
    "sort": payload.sort,
    "startDate": payload.startDate,
  });

  const headers = new Headers(compactMap({
    Accept: "application/json",
  }));

  const secConfig = await extractSecurity(client._options.apiKeyAuth);
  const securityInput = secConfig == null ? {} : { apiKeyAuth: secConfig };
  const requestSecurity = resolveGlobalSecurity(securityInput);

  const context = {
    options: client._options,
    baseURL: options?.serverURL ?? client._baseURL ?? "",
    operationID: "GetMetaEventsPaged",
    oAuth2Scopes: null,

    resolvedSecurity: requestSecurity,

    securitySource: client._options.apiKeyAuth,
    retryConfig: options?.retries
      || client._options.retryConfig
      || { strategy: "none" },
    retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
  };

  const requestRes = client._createRequest(context, {
    security: requestSecurity,
    method: "GET",
    baseURL: options?.serverURL,
    path: path,
    headers: headers,
    query: query,
    body: body,
    userAgent: client._options.userAgent,
    timeoutMs: options?.timeoutMs || client._options.timeoutMs || -1,
  }, options);
  if (!requestRes.ok) {
    return [requestRes, { status: "invalid" }];
  }
  const req = requestRes.value;

  const doResult = await client._do(req, {
    context,
    isErrorStatusCode: (statusCode: number) =>
      matchStatusCode({ status: statusCode } as Response, ["4XX", "5XX"]),
    retryConfig: context.retryConfig,
    retryCodes: context.retryCodes,
  });
  if (!doResult.ok) {
    return [doResult, { status: "request-error", request: req }];
  }
  const response = doResult.value;

  const responseFields = {
    HttpMeta: { Response: response, Request: req },
  };

  const [result] = await M.match<
    operations.GetMetaEventsPagedResponse,
    | errors.GetMetaEventsPagedBadRequestError
    | errors.GetMetaEventsPagedUnauthorizedError
    | errors.GetMetaEventsPagedNotFoundError
    | ConvoyError
    | ResponseValidationError
    | ConnectionError
    | RequestAbortedError
    | RequestTimeoutError
    | InvalidRequestError
    | UnexpectedClientError
    | SDKValidationError
  >(
    M.json(200, operations.GetMetaEventsPagedResponse$inboundSchema),
    M.jsonErr(400, errors.GetMetaEventsPagedBadRequestError$inboundSchema),
    M.jsonErr(401, errors.GetMetaEventsPagedUnauthorizedError$inboundSchema),
    M.jsonErr(404, errors.GetMetaEventsPagedNotFoundError$inboundSchema),
    M.fail("4XX"),
    M.fail("5XX"),
  )(response, req, { extraFields: responseFields });
  if (!result.ok) {
    return [result, { status: "complete", request: req, response }];
  }

  return [result, { status: "complete", request: req, response }];
}
